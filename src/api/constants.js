export const API_BASE_URL = import.meta.env.VITE_API_URL || 'https://api.example.com/v1';

export const ENDPOINTS = {
  LOGIN: '/auth/login',
  REGISTER: '/auth/register',
  SHIFTS: '/shifts',
  WORKERS: '/workers',
};